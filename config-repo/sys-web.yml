server:
  port: 3341

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000

logging:
  file: ./log/sys-web.log
  ## 集群监控使用

## turbine:
##  app-config: 
## aggregator:
## clusterConfig: default
##  cluster-name-expression: new String("default")

